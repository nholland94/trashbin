<input type="hidden"
  name="authenicity_token"
  value="<%= form_authenticity_token %>">

<label for="name">Band:</lable><br>
<input type="text"
  name="band[name]"
  value="<%= @band.name %>"
  id="name"
  placeholder="band name">

<br>

<input type="submit" value="<%= button_text %>">